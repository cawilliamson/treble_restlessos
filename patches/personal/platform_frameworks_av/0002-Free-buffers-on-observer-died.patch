From e027e37b2e751676b1390ac359e6206c5dee12b3 Mon Sep 17 00:00:00 2001
From: ChonDoit <thphantomblog@gmail.com>
Date: Fri, 14 Oct 2022 12:28:28 +0000
Subject: [PATCH 2/3] libstagefright: Free buffers on observer died

If observer died while allocating buffers, component remains
in loaded state. In cleanup, it checks for idle state to
free the allocated buffers which leads to memory leak.
Hence explicitly calling free buffers to avoid memory leak.

Change-Id: I125c336d8af3e0dd045aeba7f209edb96fb0f7dd
---
 ...fright-Free-buffers-on-observer-died.patch | 34 +++++++++++++++++++
 media/libstagefright/omx/OMXNodeInstance.cpp  |  5 +++
 2 files changed, 39 insertions(+)
 create mode 100644 0001-libstagefright-Free-buffers-on-observer-died.patch

diff --git a/0001-libstagefright-Free-buffers-on-observer-died.patch b/0001-libstagefright-Free-buffers-on-observer-died.patch
new file mode 100644
index 0000000..629e1c7
--- /dev/null
+++ b/0001-libstagefright-Free-buffers-on-observer-died.patch
@@ -0,0 +1,34 @@
+From b0a9cf66a964e4632905489bbe34bdb870c64278 Mon Sep 17 00:00:00 2001
+From: ChonDoit <thphantomblog@gmail.com>
+Date: Fri, 14 Oct 2022 12:28:28 +0000
+Subject: [PATCH 2/3] libstagefright: Free buffers on observer died
+
+If observer died while allocating buffers, component remains
+in loaded state. In cleanup, it checks for idle state to
+free the allocated buffers which leads to memory leak.
+Hence explicitly calling free buffers to avoid memory leak.
+
+Change-Id: I181c87f1fb1567071021a4265c01b7d0aabc0db4
+---
+ media/libstagefright/omx/OMXNodeInstance.cpp | 5 +++++
+ 1 file changed, 5 insertions(+)
+
+diff --git a/media/libstagefright/omx/OMXNodeInstance.cpp b/media/libstagefright/omx/OMXNodeInstance.cpp
+index 458ac9c..b49b6f7 100644
+--- a/media/libstagefright/omx/OMXNodeInstance.cpp
++++ b/media/libstagefright/omx/OMXNodeInstance.cpp
+@@ -600,6 +600,11 @@ status_t OMXNodeInstance::freeNode() {
+             break;
+     }
+ 
++    // Free any buffers leaked by an observer death during allocation.
++    // freeActiveBuffers() acquires mLock internally, so call before the
++    // outer Autolock to avoid deadlock (mLock is non-recursive).
++    freeActiveBuffers();
++
+     Mutex::Autolock _l(mLock);
+ 
+     status_t err = mOwner->freeNode(this);
+-- 
+2.53.0
+
diff --git a/media/libstagefright/omx/OMXNodeInstance.cpp b/media/libstagefright/omx/OMXNodeInstance.cpp
index 458ac9c..b49b6f7 100644
--- a/media/libstagefright/omx/OMXNodeInstance.cpp
+++ b/media/libstagefright/omx/OMXNodeInstance.cpp
@@ -600,6 +600,11 @@ status_t OMXNodeInstance::freeNode() {
             break;
     }
 
+    // Free any buffers leaked by an observer death during allocation.
+    // freeActiveBuffers() acquires mLock internally, so call before the
+    // outer Autolock to avoid deadlock (mLock is non-recursive).
+    freeActiveBuffers();
+
     Mutex::Autolock _l(mLock);
 
     status_t err = mOwner->freeNode(this);
-- 
2.53.0

