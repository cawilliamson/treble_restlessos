From a62bff9c2a0d083961d9b05a8fcfad3c1dd1c1f3 Mon Sep 17 00:00:00 2001
From: "Christopher A. Williamson" <contact@chrisaw.io>
Date: Thu, 28 May 2026 09:40:08 +0100
Subject: [PATCH 6/6] BpfNetMapsUtils and NetworkStatsService: add graceful eBPF
 fallback for broken devices

Many devices running GSIs or legacy/vendor-modified kernels do not
fully support Android's modern eBPF networking stack.

On these devices, accessing BPF maps may fail with:
- EINVAL / ENOENT (ErrnoException)
- null return from getValue() on stale/invalid maps
- RuntimeException from map wrapper on broken implementations

Previously each of these would throw from system_server, causing
firewall failures, Data Saver crashes, and NetworkStatsService restarts.

Compromise approach
=================

For BpfNetMapsUtils:
- Keep ErrnoException-first catch with Log.w() + strerror.
  This is the normal expected failure mode on broken BPF.

- Add a broader Exception catch with Log.e() + stack trace.
  This catches non-errno failures (NPE, IllegalStateException,
  IllegalArgumentException from unexpected map states) without
  crashing system_server. Still returns safe defaults:
  firewall disabled, uid rule ALLOW, data saver disabled.
  Log.e() ensures the failure is visible in bugreports.

- Add null guards for getValue() return values.
  Some broken devices return null instead of throwing.

For NetworkStatsService:
- Per-map try/catch with Log.e() instead of single blanket catch.
  This lets some BPF maps work even if others fail.
- SkDestroyListener null-guards mCookieTagMap.
- Dependencies Log.wtf() -> Log.e() so map open failures are non-fatal.

This gives the debug-builds' fallback coverage (all device-breaking
conditions handled) while avoiding the too-broad Throwable swallow
that might mask real framework bugs on working devices.
---
 .../src/android/net/BpfNetMapsUtils.java      | 64 ++++++++++++++++---
 .../server/net/NetworkStatsService.java       | 46 ++++++++++---
 2 files changed, 92 insertions(+), 18 deletions(-)

diff --git a/framework/src/android/net/BpfNetMapsUtils.java b/framework/src/android/net/BpfNetMapsUtils.java
index 287116a..06d379a 100644
--- a/framework/src/android/net/BpfNetMapsUtils.java
+++ b/framework/src/android/net/BpfNetMapsUtils.java
@@ -218,10 +218,21 @@ public class BpfNetMapsUtils {
         final long match = getMatchByFirewallChain(chain);
         try {
             final U32 config = configurationMap.getValue(UID_RULES_CONFIGURATION_KEY);
+
+            if (config == null) {
+                return false;
+            }
+
             return (config.val & match) != 0;
         } catch (ErrnoException e) {
-            throw new ServiceSpecificException(e.errno,
-                    "Unable to get firewall chain status: " + Os.strerror(e.errno));
+            android.util.Log.w("BpfNetMapsUtils",
+                    "Unable to get firewall chain status, assuming disabled: "
+                            + Os.strerror(e.errno));
+            return false;
+        } catch (Exception e) {
+            android.util.Log.e("BpfNetMapsUtils",
+                    "Unexpected error reading firewall chain state", e);
+            return false;
         }
     }
 
@@ -246,11 +257,25 @@ public class BpfNetMapsUtils {
         final boolean isAllowList = isFirewallAllowList(chain);
         try {
             final UidOwnerValue uidMatch = uidOwnerMap.getValue(new S32(uid));
-            final boolean isMatchEnabled = uidMatch != null && (uidMatch.rule & match) != 0;
-            return isMatchEnabled == isAllowList ? FIREWALL_RULE_ALLOW : FIREWALL_RULE_DENY;
+
+            final boolean isMatchEnabled =
+                    uidMatch != null && (uidMatch.rule & match) != 0;
+
+            return isMatchEnabled == isAllowList
+                    ? FIREWALL_RULE_ALLOW
+                    : FIREWALL_RULE_DENY;
+
         } catch (ErrnoException e) {
-            throw new ServiceSpecificException(e.errno,
-                    "Unable to get uid rule status: " + Os.strerror(e.errno));
+            android.util.Log.w("BpfNetMapsUtils",
+                    "Unable to get uid rule, defaulting to ALLOW: "
+                            + Os.strerror(e.errno));
+
+            return FIREWALL_RULE_ALLOW;
+        } catch (Exception e) {
+            android.util.Log.e("BpfNetMapsUtils",
+                    "Unexpected error reading uid rule", e);
+
+            return FIREWALL_RULE_ALLOW;
         }
     }
 
@@ -370,12 +395,31 @@ public class BpfNetMapsUtils {
     public static boolean getDataSaverEnabled(IBpfMap<S32, U8> dataSaverEnabledMap) {
         throwIfPreT("getDataSaverEnabled is not available on pre-T devices");
 
-        if (dataSaverEnabledMap == null) return false;
+        if (dataSaverEnabledMap == null) {
+            return false;
+        }
+
         try {
-            return dataSaverEnabledMap.getValue(DATA_SAVER_ENABLED_KEY).val == DATA_SAVER_ENABLED;
+            final U8 value =
+                    dataSaverEnabledMap.getValue(DATA_SAVER_ENABLED_KEY);
+
+            if (value == null) {
+                return false;
+            }
+
+            return value.val == DATA_SAVER_ENABLED;
+
         } catch (ErrnoException e) {
-            throw new ServiceSpecificException(e.errno, "Unable to get data saver: "
-                    + Os.strerror(e.errno));
+            android.util.Log.w("BpfNetMapsUtils",
+                    "Unable to get data saver state, assuming disabled: "
+                            + Os.strerror(e.errno));
+
+            return false;
+        } catch (Exception e) {
+            android.util.Log.e("BpfNetMapsUtils",
+                    "Unexpected error reading data saver state", e);
+
+            return false;
         }
     }
 }
diff --git a/service-t/src/com/android/server/net/NetworkStatsService.java b/service-t/src/com/android/server/net/NetworkStatsService.java
index 1553bff..d3ca591 100644
--- a/service-t/src/com/android/server/net/NetworkStatsService.java
+++ b/service-t/src/com/android/server/net/NetworkStatsService.java
@@ -678,13 +678,38 @@ public class NetworkStatsService extends INetworkStatsService.Stub {
         mInterfaceMapHelper = mDeps.makeBpfInterfaceMapHelper();
         try {
             mUidCounterSetMap = mDeps.getUidCounterSetMap();
+        } catch (Exception e) {
+            android.util.Log.e("PHH", "UidCounterSetMap unavailable", e);
+        }
+
+        try {
             mCookieTagMap = mDeps.getCookieTagMap();
+        } catch (Exception e) {
+            android.util.Log.e("PHH", "CookieTagMap unavailable", e);
+        }
+
+        try {
             mStatsMapA = mDeps.getStatsMapA();
+        } catch (Exception e) {
+            android.util.Log.e("PHH", "StatsMapA unavailable", e);
+        }
+
+        try {
             mStatsMapB = mDeps.getStatsMapB();
+        } catch (Exception e) {
+            android.util.Log.e("PHH", "StatsMapB unavailable", e);
+        }
+
+        try {
             mAppUidStatsMap = mDeps.getAppUidStatsMap();
+        } catch (Exception e) {
+            android.util.Log.e("PHH", "AppUidStatsMap unavailable", e);
+        }
+
+        try {
             mIfaceStatsMap = mDeps.getIfaceStatsMap();
-        } catch(Throwable t) {
-            android.util.Log.e("PHH", "Failed creating bpf maps", t);
+        } catch (Exception e) {
+            android.util.Log.e("PHH", "IfaceStatsMap unavailable", e);
         }
         // To prevent any possible races, the flag is not allowed to change until rebooting.
         mSupportEventLogger = mDeps.supportEventLogger(mContext);
@@ -702,13 +727,18 @@ public class NetworkStatsService extends INetworkStatsService.Stub {
                 mDeps.enabledBroadcastNetworkStatsUpdatedRateLimiting(mContext);
 
         mSkDestroyListener = mDeps.makeSkDestroyListener((message) -> {
+            if (mCookieTagMap == null) return;
+
             final StructInetDiagSockId sockId = message.inetDiagMsg.id;
+
             try {
                 mCookieTagMap.deleteEntry(new S64(sockId.cookie));
             } catch (ErrnoException e) {
-                Log.e(TAG, "Failed to delete CookieTagMap entry for " + sockId.cookie  + ": " + e);
+                Log.e(TAG, "Failed to delete CookieTagMap entry for "
+                        + sockId.cookie + ": " + e);
             }
         }, mHandler);
+
         mHandler.post(mSkDestroyListener::start);
     }
 
@@ -868,7 +898,7 @@ public class NetworkStatsService extends INetworkStatsService.Stub {
             try {
                 return new BpfMap<>(UID_COUNTERSET_MAP_PATH, S32.class, U8.class);
             } catch (ErrnoException e) {
-                Log.wtf(TAG, "Cannot open uid counter set map: " + e);
+                Log.e(TAG, "Cannot open uid counter set map: " + e);
                 return null;
             }
         }
@@ -879,7 +909,7 @@ public class NetworkStatsService extends INetworkStatsService.Stub {
                 return new BpfMap<>(COOKIE_TAG_MAP_PATH,
                         S64.class, CookieTagMapValue.class);
             } catch (ErrnoException e) {
-                Log.wtf(TAG, "Cannot open cookie tag map: " + e);
+                Log.e(TAG, "Cannot open cookie tag map: " + e);
                 return null;
             }
         }
@@ -889,7 +919,7 @@ public class NetworkStatsService extends INetworkStatsService.Stub {
             try {
                 return new BpfMap<>(STATS_MAP_A_PATH, StatsMapKey.class, StatsMapValue.class);
             } catch (ErrnoException e) {
-                Log.wtf(TAG, "Cannot open stats map A: " + e);
+                Log.e(TAG, "Cannot open stats map A: " + e);
                 return null;
             }
         }
@@ -899,7 +929,7 @@ public class NetworkStatsService extends INetworkStatsService.Stub {
             try {
                 return new BpfMap<>(STATS_MAP_B_PATH, StatsMapKey.class, StatsMapValue.class);
             } catch (ErrnoException e) {
-                Log.wtf(TAG, "Cannot open stats map B: " + e);
+                Log.e(TAG, "Cannot open stats map B: " + e);
                 return null;
             }
         }
@@ -910,7 +940,7 @@ public class NetworkStatsService extends INetworkStatsService.Stub {
                 return new BpfMap<>(APP_UID_STATS_MAP_PATH,
                         S32.class, StatsMapValue.class);
             } catch (ErrnoException e) {
-                Log.wtf(TAG, "Cannot open app uid stats map: " + e);
+                Log.e(TAG, "Cannot open app uid stats map: " + e);
                 return null;
             }
         }
-- 
2.54.0

