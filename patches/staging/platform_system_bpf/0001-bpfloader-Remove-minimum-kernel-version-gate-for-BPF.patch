From c69fb8163dcc1f0a91913abcf9e29c0c3535378c Mon Sep 17 00:00:00 2001
From: "Christopher A. Williamson" <contact@chrisaw.io>
Date: Thu, 12 Mar 2026 10:37:30 +0000
Subject: [PATCH] bpfloader: Remove minimum kernel version gate for BPF loading

Remove the min_kver check in set_skip_loading() so that all BPF
programs and maps are attempted regardless of the actual kernel
version. The real kernel version is still reported and max_kver
checks are preserved.

On kernels below 4.9 (where eBPF is not usable at all), skip
everything upfront to avoid pointless load attempts.

If a program fails to load because the kernel lacks support, the
graceful failure path in vendorBpfLoader (trebledroid patch) will
handle it. This allows kernels like 5.10 to load programs that
would otherwise be skipped due to AOSP raising the minimum in
QPR2, while still correctly skipping programs with max_kver
ceilings.

Change-Id: Id99e58f5a2c43fee9abd3b321c913f8e6e0ebbd5
---
 loader/bpfloader.rs | 22 ++++++++++++----------
 1 file changed, 12 insertions(+), 10 deletions(-)

diff --git a/loader/bpfloader.rs b/loader/bpfloader.rs
index a1ea554..a75a7c7 100644
--- a/loader/bpfloader.rs
+++ b/loader/bpfloader.rs
@@ -426,10 +426,6 @@ fn parse_release(release: &str) -> (u32, u32, u32) {
 }
 
 fn kernel_version() -> Result<u32, anyhow::Error> {
-    if let Some(kver_override) = system_properties::read("ro.bpf.kver_override")? {
-        let (major, minor, sub) = parse_release(&kver_override);
-        return Ok(kver(major, minor, sub));
-    }
     let mut buf: MaybeUninit<utsname> = MaybeUninit::zeroed();
     // SAFETY: If uname returns 0, the buf should be properly initialized.
     if unsafe { uname(buf.as_mut_ptr()) } != 0 {
@@ -453,15 +449,21 @@ fn set_skip_loading(
 ) -> Result<(), anyhow::Error> {
     let kvers = kernel_version()?;
 
+    // Skip everything on kernels below 4.9 where eBPF is not usable
+    let skip_all = kvers < kver(4, 9, 0);
+    if skip_all {
+        info!("Kernel < 4.9, skipping all BPF programs and maps");
+    }
+
     for mut map in open_file.maps_mut() {
         let name =
             map.name().to_str().ok_or_else(|| anyhow!("Failed to parse map name into UTF-8"))?;
         for map_desc in file_desc.maps {
             if map_desc.name == name {
-                if kvers < map_desc.min_kver || kvers >= map_desc.max_kver {
+                if skip_all || kvers >= map_desc.max_kver {
                     info!(
-                        "skipping map {} min_kver:{:x} max_kver:{:x} kvers:{:x}",
-                        name, map_desc.min_kver, map_desc.max_kver, kvers
+                        "skipping map {} max_kver:{:x} kvers:{:x}",
+                        name, map_desc.max_kver, kvers
                     );
                     map.set_autocreate(false)?;
                 }
@@ -475,10 +477,10 @@ fn set_skip_loading(
             prog.name().to_str().ok_or_else(|| anyhow!("Failed to parse prog name into UTF-8"))?;
         for prog_desc in file_desc.progs {
             if prog_desc.name == name {
-                if kvers < prog_desc.min_kver || kvers >= prog_desc.max_kver {
+                if skip_all || kvers >= prog_desc.max_kver {
                     info!(
-                        "skipping program {} min_kver:{:x} max_kver:{:x} kvers:{:x}",
-                        name, prog_desc.min_kver, prog_desc.max_kver, kvers
+                        "skipping program {} max_kver:{:x} kvers:{:x}",
+                        name, prog_desc.max_kver, kvers
                     );
                     prog.set_autoload(false);
                 }
-- 
2.53.0

